apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: git-shared
spec:
  storageClassName: seaweedfs-extra
  volumeName: git-shared
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
