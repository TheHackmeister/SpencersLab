apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: documents-shared
spec:
  storageClassName: seaweedfs-important
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
