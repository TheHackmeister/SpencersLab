apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-shared
spec:
  storageClassName: seaweedfs-storage
  volumeHandle: media-shared
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
