apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: player-shared
spec:
  storageClassName: seaweedfs-storage
  volumeName: player-shared
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
