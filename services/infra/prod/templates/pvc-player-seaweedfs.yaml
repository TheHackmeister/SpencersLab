apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: player-shared
spec:
  storageClassName: seaweedfs-storage
  volumeName: player-shared
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 30Gi
