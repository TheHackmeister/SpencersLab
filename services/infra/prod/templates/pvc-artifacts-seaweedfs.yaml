apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: artifacts-shared
spec:
  storageClassName: seaweedfs-storage
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
