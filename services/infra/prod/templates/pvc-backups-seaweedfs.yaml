apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backups-shared
spec:
  storageClassName: seaweedfs-backups
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 32Gi
