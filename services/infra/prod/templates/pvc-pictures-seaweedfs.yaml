apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pictures-shared
spec:
  storageClassName: seaweedfs-important
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
