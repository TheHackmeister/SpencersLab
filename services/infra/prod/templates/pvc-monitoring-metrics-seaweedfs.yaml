apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: monitoring-metrics-shared
spec:
  storageClassName: seaweedfs-storage
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
